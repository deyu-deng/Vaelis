fastapi>=0.110
uvicorn[standard]>=0.29
httpx[http2]>=0.27
pyyaml>=6.0
cryptography>=42.0          # encrypted vault (Fernet) — required
# optional extras (also in pyproject.toml):
#   pip install ".[vault]"    -> keyring (OS keyring backend for the vault)
#   pip install ".[discover]" -> mitmproxy (protocol capture addon)
#   pip install ".[protobuf]" -> protobuf (generate cursor_pb2 stubs)
