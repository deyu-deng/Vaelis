name: vaelis-agenda
version: "0.1.0"
description: >-
  Resolves agenda confirmations ("确认 3" / "忽略 3") straight from a chat
  message, without waking the agent. Keeps the confirm loop free of model
  tokens, which matters because L1 runs a flagship model (ADR-0011).
author: Vaelis
hooks:
  - pre_gateway_dispatch
